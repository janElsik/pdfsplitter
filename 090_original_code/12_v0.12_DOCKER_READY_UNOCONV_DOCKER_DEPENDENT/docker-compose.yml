version: '3.5'
services:
  go-server:
    build:
      args:
        - ENV=prod
      context: .
    image: pdfsplitter_without_unoconv
    container_name: pdfsplitter_without_unoconv
   # ports:
    #  - "4445:9000"